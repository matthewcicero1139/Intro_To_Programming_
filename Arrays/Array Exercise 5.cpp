// NOT FINISHED

// user input 10 numbers into array, display largest and smallest values
#include <iostream>
int main()
{
	// Getting input from user
	int a, b, c, d, e, f, g, h, i, j;
	std::cin >> a >> b >> c >> d >> e >> f >> g >> h >> i >> j;

	// Array called snipe 
	int snipe[] = { a,b,c,d,e,f,g,h,i,j };

	// trying to make it print largest and smallest numbers
	// trying an if statement
	
	//use a loop to find largest and smallest
	for(int k = 0 )
	






//	std::cout << a << b << c << d << e << f << g << h << i << j;

	system("pause");
}